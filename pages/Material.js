import {Text} from "react-native";
import * as React from "react";

const Material = ({navigation, route}) => {
    return (
        <Text>Здесь будет задание 9 </Text>
    )
};

export default Material;